/// Web stub — [ApiBinding] uses in-memory [CookieJar] instead; this is never called.
Future<String> cookieJarStoragePath() async {
  throw UnsupportedError('cookieJarStoragePath is only used on dart:io platforms');
}
